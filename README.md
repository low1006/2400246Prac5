# 2400246Prac5

## Configuration
**Update:** As part of the recent adaptive maintenance, this system now requires configuration settings to be stored in a `config.json` file format. The legacy `config.txt` format is no longer supported.